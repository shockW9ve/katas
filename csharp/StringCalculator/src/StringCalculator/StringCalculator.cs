namespace StringCalculator;

public class StringCalculator
{
    public int Add(string input)
    {
        if (input.Length == 0)
        {
            return 0;
        }

        // parse header
        // build delimter
        // tokenize
        // numerize

        return 9;
    }
}
