import { MovementStatus } from "../helpers/status";
import { MarsRover } from "../models/marsrover";
import { Plateau } from "../models/plateau";

// export function execute(rover: MarsRover, plateau: Plateau): MovementStatus {
//   return { position: Position, status: Status };
// }
// m
//

tryMove(){
    // TODO: Move to service and do trymove with delta and then pass the position here
    let delta = directionalDelta(rover.heading);
}
