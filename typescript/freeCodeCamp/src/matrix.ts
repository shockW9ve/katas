let matrix = [
  [1, 2, 3],
  [4, 5, 6],
];

function transpose(matrix: Array<number>): Array<number> {
  const transposedMatrix: Array<number> = [];

  if (matrix.length < 1) {
    return [];
  }

  for (let numbers of matrix) {
  }

  return transposedMatrix;
}
