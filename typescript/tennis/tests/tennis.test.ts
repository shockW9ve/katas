import { describe, it, expect } from "vitest";
import { Points, applyPoint } from "../src/Game/TennisGame.js";

describe("", () => {
  it("", () => {
    const p = Object.freeze({ a: 0, b: 0 }) as Points;
    const next = applyPoint(p, "A");
  });
});
